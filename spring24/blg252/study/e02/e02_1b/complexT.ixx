// complexT.ixx for the example e02_1b.zip
// Module interface
// Contains decleration and definition of ComplexT
// ComplexT is used to define complex numbers

export module ComplexT;

export struct ComplexT {      	// Declaration (type) of complex numbers
	double re{}, im{};		    // real and imaginary parts initialized to zero
};
